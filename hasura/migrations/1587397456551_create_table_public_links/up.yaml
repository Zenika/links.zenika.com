- args:
    cascade: false
    read_only: false
    sql: CREATE EXTENSION IF NOT EXISTS pgcrypto;
  type: run_sql
- args:
    cascade: false
    read_only: false
    sql: CREATE TABLE "public"."links"("id" uuid NOT NULL DEFAULT gen_random_uuid(),
      "incoming_link" text NOT NULL, "outgoing_link" text NOT NULL, PRIMARY KEY ("id")
      , UNIQUE ("incoming_link"));
  type: run_sql
- args:
    name: links
    schema: public
  type: add_existing_table_or_view
